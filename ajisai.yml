package:
  name: sushichan044-ai-presets
  exports:
    essentials:
      rules:
        - "essentials/rules/**/*.md"
      prompts:
        - "essentials/prompts/**/*.md"
    tdd:
      rules:
        - "tdd/rules/**/*.md"
      prompts:
        - "tdd/prompts/**/*.md"
    typescript:
      rules:
        - "typescript/rules/**/*.md"
      prompts:
        - "typescript/prompts/**/*.md"
